#ifndef ENTIDADE_HPP
#define ENTIDADE_HPP

// Nome, telefone, email, senha, CPF.
class Usuario{
private:
    Nome nome = Nome(nome);
    Telefone telefone = Telefone(telefone);
    Email email = Email(email);
    Senha senha = Senha(senha);
    Cpf cpf = Cpf(cpf);
public:
    Usuario(Nome nome, Telefone telefone, Email email, Senha senha, Cpf cpf);
};

#endif